import Ember from 'ember';
//Need to call on all model 'tours' in this route in order to display them in display-cart template?
export default Ember.Route.extend({
  // model() {
  //   return this.store.findAll('tour');
  // }
  //end
});
